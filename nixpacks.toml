[phases.setup]
nixPkgs = ["chromium", "nss", "atk", "cups", "dbus", "expat", "libdrm", "libxkbcommon", "mesa"]

[phases.install]
cmds = ["pip install -r requirements.txt", "python -m playwright install chromium", "python -m playwright install-deps chromium"]
